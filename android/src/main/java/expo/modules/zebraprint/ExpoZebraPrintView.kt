package expo.modules.zebraprint

import android.content.Context
import expo.modules.kotlin.AppContext
import expo.modules.kotlin.views.ExpoView

// Placeholder view class - not currently used by the ExpoZebraPrint module
// The module only exposes functions for printer interaction, no UI components
class ExpoZebraPrintView(context: Context, appContext: AppContext) : ExpoView(context, appContext)
